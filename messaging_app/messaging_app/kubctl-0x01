#!/usr/bin/env bash
set -euo pipefail

DEPLOYMENT="messaging-app"

kubectl scale deployment "$DEPLOYMENT" --replicas=3
kubectl get pods -l app=messaging-app
kubectl top pods

# Optional load test
# wrk -t2 -c50 -d30s http://<minikube-ip>:<nodeport>/
